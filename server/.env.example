# Copy this file to .env and fill in values
# Database
DB_USER=postgres
DB_PASSWORD=your_db_password
DB_HOST=localhost
DB_NAME=todoapp
DB_PORT=5432

# Server
PORT=3000

# JWT
JWT_SECRET=your_jwt_secret_here

# NODE_ENV (optional)
NODE_ENV=development

# Note: Do NOT commit a real .env with secrets to source control
